package com.collect.it.googleplus;

import com.google.android.gms.plus.model.people.Person;

/**
 * This interface defines the call backs for the Google plus functionality
 */
public interface OnGooglePlusProcess {
	/**
	 * this method will call if the connection is established and gives the
	 * information of logged in user
	 */
	void onGooglePlusPerson(Person person);

}
